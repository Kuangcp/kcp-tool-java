dependencies {
  api project(':kcp-core')
  api project(':kcp-aop')
  api project(':kcp-mock')
  api project(':kcp-tuple')
  api project(':kcp-math')
  api project(':kcp-kafka')
  implementation(project(':common-config'))
}